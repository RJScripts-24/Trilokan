# ============================================================================
# Requirements for identity-verifier service
# IMPORTANT: Set environment variable CMAKE_BUILD_PARALLEL_LEVEL=1 to reduce build memory
# ============================================================================

# Web Framework
Flask==3.1.2
Werkzeug==3.1.3

# Core Scientific Computing - Use versions with pre-built wheels
numpy==1.26.4  # Has pre-built wheels for Linux
scipy==1.11.4  # Has pre-built wheels, avoid 1.12+ which may require compilation
pandas==2.1.4  # Stable version with wheels

# Computer Vision & Image Processing (headless version is smaller)
opencv-python-headless==4.8.1.78
Pillow==10.3.0

# PyTorch CPU-only (much smaller than GPU, and TensorFlow removed as it's not used)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu
scikit-learn==1.3.2  # For ROC analysis, metrics, and fusion models

# Deep Learning - CNN Deepfake Detection (Phase 1 & 2)
timm==0.9.16  # PyTorch Image Models for Xception/EfficientNet architectures
captum==0.7.0  # Model interpretability, Grad-CAM, and explainability

# Face Recognition & dlib
# NOTE: dlib requires C++ compilation. Set CMAKE_BUILD_PARALLEL_LEVEL=1 in environment
dlib==19.24.6  # Last stable version with good wheel support
face_recognition==1.3.0

# Audio Processing
librosa==0.10.1  # Use specific version with pre-built wheels
soundfile==0.12.1
webrtcvad==2.0.10

# Visualization & Monitoring
matplotlib==3.8.4  # For ROC curves, calibration plots, and visualization
tqdm==4.66.4

# Document Processing
pytesseract==0.3.10

# Utility Libraries
python-dotenv==1.0.0
python-dateutil==2.9.0
pytz==2024.1

